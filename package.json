{
  "name": "xc-game",
  "version": "0.0.0",
  "scripts": {
    "build": "tsconfig && tsc",
    "test": "mocha",
    "buildtest": "tsconfig && tsc && mocha",
    "serve": "http-server -c-1"
  },
  "dependencies": {
    "three": "*"
  },
  "devDependencies": {
    "http-server": "*",
    "mocha": "*",
    "tsconfig-glob": "*",
    "typescript": "*"
  },
  "private": true,
  "license": "GPLV2"
}
