{
  "name": "xc-game",
  "version": "0.0.0",
  "scripts": {
    "build": "browserify  --debug src/main.js -o static/bundle.js -t babelify",
    "watch": "watchify -v --debug src/main.js -o static/bundle.js -t babelify",
    "serve": "http-server -c-1 -o"
  },
  "dependencies": {
    "three": "*"
  },
  "devDependencies": {
    "babel-plugin-transform-flow-strip-types": "^6.21.0",
    "babel-preset-es2015": "*",
    "babelify": "*",
    "browserify": "*",
    "http-server": "*",
    "watchify": "*"
  },
  "private": true,
  "license": "GPLV2"
}
