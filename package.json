{
  "name": "xc-game",
  "version": "0.0.0",
  "scripts": {
    "build": "browserify  --debug src/main.js -o bundle.js -t babelify",
    "watch": "watchify -v --debug src/main.js -o bundle.js -t babelify",
    "serve": "http-server -c-1 -o"
  },
  "dependencies": {
    "axiom": "^0.1.6",
    "gl-matrix-vec3": "*",
    "three": "*",
    "zepto": "^1.0.1"
  },
  "devDependencies": {
    "babel-preset-es2015": "*",
    "browserify": "*",
    "watchify": "*",
    "babelify": "*",
    "http-server": "*"
  },
  "private": true,
  "license": "GPLV2"
}
